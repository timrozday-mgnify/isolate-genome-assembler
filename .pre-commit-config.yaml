default_stages: [pre-commit]

# Vendored nf-core modules are kept byte-identical to the modules repo so `nf-core modules
# lint` and `nf-core modules update` stay clean; the whitespace hooks must not rewrite them.
exclude: '^modules/nf-core/'

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-json
      - id: check-added-large-files
        args: [--maxkb=5120]
      - id: mixed-line-ending
        args: [--fix=lf]
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.12.11
    hooks:
      - id: ruff-check
      - id: ruff-format
  - repo: local
    hooks:
      - id: nextflow-preview
        name: nextflow preview
        entry: nextflow run main.nf -preview
        language: system
        pass_filenames: false
        files: '\.(nf|config|ya?ml)$'
